---
id: ai-industry-kb-2026/07-kv-cache-long-context-techniques/quantized-kv-in-production-new-evidence-and-the-per-engine-d
title: "Quantized KV in production: new evidence and the per-engine dtype matrix (Wave 2.1)"
domain: kv-cache-long-context-techniques
role: deep-dive
task: architecture
actors: ["AMD", "Alibaba", "Apple", "DeepSeek", "Google", "Intel", "Moonshot", "Nvidia", "SGLang", "Samsung", "TensorRT-LLM", "vLLM"]
dates: ["2025-05", "2026-03-21", "2026-03-25", "2026-06"]
keywords: ["agent", "agentic", "agents", "amd", "attention", "benchmark", "benchmarks", "cost", "decode", "deepseek", "disaggregated", "fp4"]
source: docs/RAG/ai-industry-knowledge-base-2026.md
source_anchor: ""
source_lines: [3445, 3467]
section: "7. KV Cache & Long-Context Techniques"
sha256: f27bb40dd712782470228ad5e7fb9cbc0585568c9549b52505846c5c613458a9
---

# Quantized KV in production: new evidence and the per-engine dtype matrix (Wave 2.1)

- **vLLM**: automatic prefix caching — shared system prompts and few-shot prefixes computed once; PagedAttention's block structure made prefix hits cheap.
- **SGLang**: **RadixAttention** — a radix tree over token sequences enabling cache-aware scheduling (requests routed to the instance already holding their longest matching prefix) and cross-request prefix reuse, plus jump-forward speculative decoding on the same tree.
- **Mooncake / Mooncake Store / LMCache-class systems**: disaggregated KV storage persisting cache blocks beyond a single request's lifetime — repeated prompts (agents looping over the same repo, RAG pipelines) skip prefill entirely.
- **Mooncake** (Moonshot AI's serving platform for Kimi; arXiv:2407.00079) is the production reference for KVCache-centric disaggregation: three disaggregated resource pools, objective of maximizing effective throughput subject to TTFT/TBT SLOs. Measured: **+115% more requests on A800 clusters, +107% on H800** vs the prior system (same SLOs); **50–525% throughput gains** at matched SLOs on simulated data (conversation workload: +498% at 100 ms TBT); **+75% more requests** replaying real request traces (10 prefill + 10 decode vs 20 converged vLLM instances). Mechanisms: chunked pipeline parallelism (CPP), layer-wise prefill with stream transfer, KVCache-centric scheduling, heuristic hotspot migration, overload-oriented accept/reject scheduling.
- Mooncake Store's open-source descendant as an LMCache/vLLM backend (Tsinghua ToS 2025 evaluation, Qwen2.5-7B-Instruct-GPTQ-Int4 on A10, 200 requests): TTFT **17–22% reduction over Redis on TCP; 26–33% on RDMA** — the measured price of the transport layer inside an otherwise identical disaggregated stack.
- **Economics**: production traces show **95%+ cache-hit ratios** on agentic workloads, which is why providers now price *cached* input tokens separately. Worked example (OpenCode-style /data metrics, June 2026): an agent loop making 20 calls against a 100K-token repo context pays full prefill once (~100K uncached tokens) plus ~1.9M cached tokens — at typical 10:1 cached:uncached pricing, the *effective* input cost drops ~9× vs no caching. Prefix reuse composes multiplicatively with compression: a 6×-compressed cache reused across 20 agent turns is effectively a ~100× prefill saving.

### Quantized KV in production: new evidence and the per-engine dtype matrix (Wave 2.1)

- **vLLM** (2026 mainline): `--kv-cache-dtype` supports `auto | fp8 | fp8_e4m3 | fp8_e5m2`. Production DeepSeek-R1 / DeepSeek-V4-Flash serving recipes standardize on **`--kv-cache-dtype fp8_e4m3`** (paired with `--dtype fp8`) — the production dtype pinned from Wave 2's generic "FP8" finding. Operational flags: `--kv-cache-dtype-skip-layers` (keep sensitive layer types, e.g. sliding-window, at bf16); `--mamba-cache-dtype` accepts only auto/bfloat16/float16/float32 (no FP8) — GDN recurrent state is ~0.02 GiB, so the limitation is a non-issue.
- **SGLang — the broadest 2026 matrix**: `--kv-cache-dtype` supports **`fp8_e5m2 | fp8_e4m3 | fp4_e2m1 | nvfp4 | fp4_mx_block16 | int8 | int4 | int2`** (docs, Sep 2026). FP8 E5M2 (±57,344 range) vs E4M3 (±240, more precision) is the precision-vs-dynamic-range knob. **FP4 (MXFP4 E2M1, per-block-16 microscaling) is experimental**; scales computed on-the-fly, no pre-quantized checkpoint needed; SM100-native recipes via `--prefill-kv-cache-dequant-dtype`. INT8/INT4/INT2 are Triton-based with dynamic affine per-token scales and `--kv-cache-quant-group-size`. **Capacity accounting (SGLang docs): FP4 ≈ 1.78× more tokens than FP8, ≈ 3.56× more tokens than BF16** (scale overhead included).
- **TensorRT-LLM**: FP8 KV-cache support widely documented in NVIDIA materials; exact introduction release [UNVERIFIED] — "documented, version unpinned."
- **TGI counter-case**: HF TGI (archived read-only **2026-03-21**, maintenance mode) never gained mainline-CUDA MLA support — MLA work existed only on the Intel-Gaudi branch. "All engines" framings of MLA support must read 3/4 (vLLM, SGLang, TensorRT-LLM), not 4/4. (MLA mechanisms themselves are covered in part 07a; only the engine-matrix verdict is carried here.)
- **vLLM Apr-2026 FP8-KV benchmarks** (the production-default evidence): FP8 KV reduces the **inter-token-latency slope to 54% of the BF16 baseline on H100**, **break-even starting at ~7K tokens**, **sub-0.3% accuracy cost** on standard benchmarks. Calibrated scales (e.g. via llm-compressor) beat naive per-tensor scales; `fp8_e4m3` vs `fp8_e5m2` is worth trying when subtle separators degrade.
- **The 2026 FP8 landmine**: Hopper's FP8 Tensor Cores suffer intermediate accumulation precision loss when the contraction dimension is large — in early FP8-KV implementations this collapsed **128K needle-in-haystack accuracy from 91% → 13%** with no error logs, no OOM, no visible failure. Fix: **two-level FP32 accumulation** (FlashAttention-3 patches `flash-attention#96`, `#91`). Canary: **NIAH recall < 50% at 128K after enabling FP8 KV = this bug; do not route production traffic until the patch is confirmed present.**
- **Production-adoption datapoints, 2025–2026**: DeepSeek's own **FlashMLA (Feb 2025)** — custom FP8 KV format (656 bytes/token) — is the origin point, consumed by both vLLM and SGLang. **SGLang FlashMLA + FP8 KV + MTP (May 2025, PR #6109)**: ~30% serving speedup, KV cache halved on DeepSeek-V3/R1-class models. **SemiAnalysis InferenceX (Dec 2025–Jan 2026)**: DeepSeek-R1 NVFP4 on B200 via SGLang 0.5.6 — **907 tok/s/GPU at concurrency 4** (1.79× over 0.5.5). 2026 vLLM production recipes for DeepSeek-R1 (8× GH200/H200, DP+EP) and DeepSeek-V4-Flash (4× GH200) standardize on `fp8_e4m3` with MLA attention backends; the DP+EP note matters because MLA requires DP+EP for KV-cache efficiency (avoids 8× duplication across TP ranks).
- Engine-divergence note [SECONDARY, HARDWARE-SPECIFIC]: one AMD-fork benchmark found fp8_e4m3 KV faster *and* 2× capacity on SGLang (fused fp8-KV kernels) while the same GPU under vLLM-ROCm showed fp8 KV halving decode vs fp16 (unfused Triton dequant fallback). FP8-KV performance is kernel-fusion-dependent, not a dtype property.
- **TurboQuant** (Google Research, ICLR 2026, blog 2026-03-25): 3-bit keys / 2-bit values, **≥6× KV memory reduction**, **up to 8× attention-logit speedup on H100**, 99.5% attention fidelity, lossless needle-in-haystack, no calibration — the 2026 reference for online calibration-free KV quantization. Market event on announcement: Samsung −4.7%, SK Hynix −6.2%, Micron −3.4%. Not yet upstream in vLLM as of the waves (community integrations: `0xSero/turboquant` Triton kernels + vLLM integration; turboquant-mlx on Apple Silicon).
- The brief's phrase "dynamic drift correction" names **no single documented method** [UNVERIFIED] — the documented mechanisms are the error-correction family covered in part 07a cross-refs (GEAR's X ≈ D̂ + L + S decomposition, MiKV's *runtime* Dynamic Outlier Awareness, ResQ, WKVQuant, KVLinC, Kitty, FoveatedKV, TurboQuant's QJL residual correction).
- **FP8-KV production checklist (2026)**: default to `fp8_e4m3`; pin the FlashAttention-3 two-level FP32 accumulation patches; needle-test every enablement (128K NIAH probe; <50% = the bug, not the model); use `--kv-cache-dtype-skip-layers` for sensitive layers; calibrate rather than guess scales; budget the next step as INT4 KV (KIVI-class) or TurboQuant-class 3-bit.

### Million-token contexts: normalized among leaders — with tier exceptions (Waves 1–3)

